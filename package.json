{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "sh -c \"(cd src-tauri && cargo tauri dev) \"",
    "icon": "sh -c \"(cd src-tauri && cargo tauri icon ../src/logo.svg) \"",
    "build": "sh -c \"(cd src-tauri && cargo tauri build) && (mv src-tauri/target/release/*.exe target/) && (mv src-tauri/target/release/bundle/nsis/*setup.exe target/) \""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.17.0",
      "onFail": "download"
    }
  },
  "type": "module"
}
