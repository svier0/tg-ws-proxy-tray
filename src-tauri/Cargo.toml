[package]
name = "tg-ws-proxy-tray"
version = "0.1.0"
description = "tg-ws-proxy-tray"
authors = ["心衍"]
edition = "2021"

[lib]
name = "tg_ws_proxy_tray_lib"
crate-type = ["rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# simple-tauri = "0.2"
simple-tauri = { path = "../../simple-tauri/" }
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "2"
indoc = "2"

# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = 'thin'
opt-level = 'z'
panic = "abort"
strip = true
